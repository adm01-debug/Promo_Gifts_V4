import { describe, expect, it } from 'vitest';

describe('vitest config smoke', () => {
  it('loads the Vitest 4 config without deprecated poolOptions', () => {
    expect(true).toBe(true);
  });
});
