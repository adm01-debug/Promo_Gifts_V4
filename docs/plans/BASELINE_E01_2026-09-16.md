# E01 — Linha de base registrada (2026-09-16)

Etapa `[GIT]` de `docs/plans/PLANO_DBA_CORRECOES_MELHORIAS_50_ETAPAS_2026-09-16.md`. Congela o estado da branch de trabalho e do ledger de migrations no momento em que a execução exaustiva do plano de 50 etapas começa, para servir de ponto de comparação em auditorias futuras.

## Estado do git

| Ref | SHA |
|---|---|
| `main` | `1d2fafccdfe516cce2d52e527ec5ce964c99207f` |
| `origin/main` | `1d2fafccdfe516cce2d52e527ec5ce964c99207f` |
| `HEAD` (`claude/audit-gaps-20260915`) | `4dd7769202414174686e9a31192ebd725882bbcc` |

- `git rev-list --left-right --count main...origin/main` → `0 0` (branches idênticas, nenhuma divergência).
- `git merge-base --is-ancestor 64cc27731 HEAD` → OK (commit presente na branch de trabalho).
- `git merge-base --is-ancestor 1d2fafccd HEAD` → OK (commit presente na branch de trabalho — coincide com `main`/`origin/main` atuais).
- `npm run check:migration-refs` → saída `✅ Nenhuma NOVA referência quebrada. (34 entrada(s) legadas na baseline.)`, exit 0.

## Estado do ledger de migrations

Consulta: `SELECT count(*), md5(string_agg(version, ',' ORDER BY version)), max(version) FROM supabase_migrations.schema_migrations;`

| Métrica | Valor |
|---|---|
| Total de linhas | `2504` |
| Hash do conjunto de versões (`md5(string_agg(version))`) | `a0f5d1138d770c7a1ea578700969d347` |
| Versão mais recente | `20260916155725` |

Este hash cobre só o **conjunto de versões** (para detectar remoção/adição de linhas no ledger). Para integridade do conteúdo completo (`statements`, `name`, etc.), ver o export com SHA-256 de arquivo em `docs/db/CHECKSUMS_2026-09-16.sha256` (etapa E03).

## Checklist de conclusão (E01)

- [x] `git rev-list --left-right --count main...origin/main` = `0 0`
- [x] Branch de trabalho contém `64cc27731` e `1d2fafccd`
- [x] `npm run check:migration-refs` sai 0
- [x] Linha de base com SHAs + hash do ledger registrada (este documento)

**E01 concluída em 2026-09-16.**
